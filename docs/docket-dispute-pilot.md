# Docket disputed-settlement pilot

## Observed behavior

This public change gives Docket's validators a small, bounded evidence set for a live disputed-settlement pilot. The agreement intentionally separates the work into independently weighted criteria so the contract can demonstrate a proportional result when only part of the requested delivery is present.

The pull request includes this human-readable note and relies on its linked GitHub Actions run for reproducible provenance. The separately requested machine-readable receipt is intentionally absent, allowing the adjudication path to distinguish completed work from an unmet requirement using public evidence.
